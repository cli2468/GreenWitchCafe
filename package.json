{
  "name": "green-witch-cafe-root",
  "version": "1.0.0",
  "description": "Root package to forward commands to the frontend.",
  "scripts": {
    "dev": "cd cafe-frontend && npm run dev",
    "build": "cd cafe-frontend && npm run build",
    "install": "cd cafe-frontend && npm install"
  }
}
